import numpy as np

a = np.array([1, 2, 3])
print(a.shape)     #get shape of a
# (3, ) means i has 3 elements

b = np.array([[1, 2, 3],[4, 5, 6]])
print(b.shape)     #get shape of b
# (2, 3) means 2 row 3 columns
